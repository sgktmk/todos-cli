#!/usr/bin/env python3
"""リポジトリから直接実行するための起動スクリプト。"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from todos.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
